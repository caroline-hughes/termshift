# Copy this file to `.env.local` for local development.
# Do not commit real keys. Vercel should store the same name in project env vars.
# Used for job/transcript extraction and optional Plan Coach. The app works without it.

OPENAI_API_KEY=
